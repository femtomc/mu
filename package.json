{
	"name": "mu-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit && tsc -p packages/web/tsconfig.json --noEmit",
		"clean": "rm -rf packages/core/dist packages/forum/dist packages/issue/dist packages/orchestrator/dist packages/cli/dist",
		"build": "bun run clean && bun run build:packages && chmod +x packages/cli/dist/cli.js",
		"build:packages": "tsc -p packages/core/tsconfig.build.json && tsc -p packages/forum/tsconfig.build.json && tsc -p packages/issue/tsconfig.build.json && tsc -p packages/orchestrator/tsconfig.build.json && tsc -p packages/cli/tsconfig.build.json",
		"pack:smoke": "bun run build && bun run scripts/pack-smoke.ts",
		"fmt": "biome format --write .",
		"lint": "biome lint .",
		"web:dev": "cd packages/web && bun run dev",
		"web:build": "cd packages/web && bun run build",
		"web:test": "bun run web:build && cd packages/web && bun run e2e",
		"check": "biome check --write --error-on-warnings . && bun run typecheck && bun run build && bun test"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.5",
		"bun-types": "^1.3.9",
		"typescript": "^5.9.2"
	},
	"dependencies": {
		"mu-monorepo": "."
	}
}
