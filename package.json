{
	"name": "mu-monorepo",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"fmt": "biome format --write .",
		"lint": "biome lint .",
		"check": "biome check --write --error-on-warnings . && bun run typecheck && bun test"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.5",
		"bun-types": "^1.3.9",
		"typescript": "^5.9.2"
	}
}
